# A simple CLI code helper
## Usage: main.py <prompt> [--verbose]
A simple AI agent using Gemini, contained to calculator project directory. Can read, and edit files within that directory in response to the prompt.
